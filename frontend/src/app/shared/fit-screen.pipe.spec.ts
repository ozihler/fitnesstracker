import { FitScreenPipe } from './fit-screen.pipe';

describe('FitScreenPipe', () => {
  it('create an instance', () => {
    const pipe = new FitScreenPipe();
    expect(pipe).toBeTruthy();
  });
});
